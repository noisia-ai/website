# Demo deck (sanitized)

Reference deck assembled from the kit. To render it:

```bash
cp ../../engine/{noisia-tokens.css,deck.css,deck-stage.js} ../../assets/logo_norm.svg .
node ../../builders/build-pdf.mjs index.html demo.pdf
```

Generic Noisia content only — no client data. See ../../AGENTS.md.
